---
type: regex
target: last_message
match: contains
pattern: "(?=[\\s\\S]*\\.handovers/)(?=[\\s\\S]*OMNI-8)"
---

The reply narrates the note (naming the `.handovers/` path) and carries the
focus thread (OMNI-8, the settings-page polish) into it.
