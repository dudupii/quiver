---
type: regex
target: last_message
match: contains
pattern: "ZULU-9"
---

The numeric argument widens the window to 4, pulling the oldest seeded note
(the ZULU-9 Rust spike) into scope — its unique marker must now reach the
brief. Complements catchup-brief's not_contains: together they pin the
window's default and its override.
