---
type: regex
target: trace
match: not_contains
pattern: "\"command\"\\s*:\\s*\"(?:[^\"\\\\]|\\\\.)*claude(?:[^\"\\\\]|\\\\.)*--bg"
---

The prompt never asks for relay — a session wrap-up with the work
explicitly deferred — so no background launch may be invoked anywhere in
the run, stub or real. This is the autofire pin: frontmatter and adapter
policy claim it, this grader proves it.
