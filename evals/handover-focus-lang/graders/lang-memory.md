---
type: regex
target: files
match: count:1
pattern: "\\.handovers/\\.lang"
---

The language token `zh` was passed (alongside a focus), so `.handovers/.lang`
is persisted as usual — the focus must not suppress language memory.
