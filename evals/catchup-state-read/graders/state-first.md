---
type: regex
target: last_message
match: contains
pattern: "NOVEMBER-9"
---

The state file is read first: NOVEMBER-9's source note (2026-02-10) is
outside every plausible reading window, so the fact can only reach the
brief through CURRENT.md. A run that ignores the projection and replays
notes loses install-era facts — this grader is the design's reason to
exist.
