---
type: regex
target: last_message
match: contains
pattern: "(?=[\\s\\S]*Alfa)(?=[\\s\\S]*Bravo)"
---

Parallel work is visible: the brief says the newest-date group holds more
than one note and names the authors (frontmatter `author` is the only
place these names live, so plain filename citation cannot satisfy this
accidentally).
